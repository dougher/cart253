//println(distX + " < distX | distY > " + distY);
//println(speedX + " < speedX | speedY > " + speedY);
//println(newX + " < newX | newY > " + newY);

 /* 
 if (ticks >= CLOCK && !moving) {
      action = (int) random(1, 3);

    } else if (ticks >= CLOCK) {

    } else 
    */